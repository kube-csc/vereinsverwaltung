require('./bootstrap');

// Alpine.js wird ab Livewire 3 automatisch mitgeliefert (@livewireScripts)
// und darf nicht mehr manuell eingebunden werden, da es sonst zu
// Konflikten kommt (z.B. "Alpine.reactive is not a function").

//boxicons
require('boxicons/dist/boxicons.js');
